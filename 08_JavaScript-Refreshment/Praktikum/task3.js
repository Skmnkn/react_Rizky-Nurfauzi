const foo = ["Budi", "Sista", "Ayu"];
const [a, b, c] = foo;
console.log(`${a} tersimpan dalam variable a`);
console.log(`${b} tersimpan dalam variable b`);
console.log(`${c} tersimpan dalam variable c`);